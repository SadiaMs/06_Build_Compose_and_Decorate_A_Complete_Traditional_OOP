class Logger:
    def __init__(self):
        print("Logger created: Constructor called.")

    def __del__(self):
        print("Logger destroyed: Destructor called.")

# Example usage:
log = Logger()

# To explicitly delete the object (so destructor is triggered immediately)
del log
